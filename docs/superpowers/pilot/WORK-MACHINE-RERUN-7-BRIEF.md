# Бриф №7: re-run после M12 (рабочая машина) — symbol-агрегация ранжирования

> Задание для Claude Code-сессии на рабочей машине. Пользователь скажет: «прогони re-run по
> этому брифу». Baseline — re-run №5/№6 + MCP-пилот §3.2/§4.1. M12 добавил symbol-агрегацию:
> search_code возвращает k РАЗЛИЧНЫХ символов (sibling-чанки класса схлопнуты в best-rank
> представитель + поле `sibling_chunks`), пул кандидатов ×4. Смоук: `git pull` (HEAD ≥ e85a497)
> → `docker compose up -d` → `uv sync --extra local-emb` → `uv run pytest -m "scip and falkordb"
> tests/eval/test_m10_gate.py -q --junit-xml=/tmp/g.xml`. Индекс — тёплый кэш (~35-40s).

## 1. ГЛАВНЫЙ чекпойнт — hit@8 на 8 RU-вопросах (был 5/8; ОЖИДАНИЕ 7/8)

`eval retrieval --exact` на тех же 8 вопросах (`ru-questions.yaml` MCP-пилота). Ожидание —
именно ОЖИДАНИЕ, не assertion:
- Два §4.1-промаха («загрузка файла», «конфигурация URL») должны закрыться: sibling-чанки
  `DocumentStorageClient`/`ServicesSettings` теперь одна позиция → целевой метод-чанк входит
  в top-8. Зафиксировать новые ранги обоих.
- Клиент/сервер-промах («связанные клиенты v2») — БЕЗ изменений ожидаемо (это не гранулярность;
  service-хинт остаётся рабочим обходом).
- 5 прежних попаданий — ранги могут сдвинуться (пул ×4 — легитимное расширение кандидатов);
  фиксировать все 8 рангов таблицей против №5.

Если hit@8 НЕ вырос — разобрать по-нашему: какие чанки заняли top-8, что показывает
`sibling_chunks`/`enclosing_symbol` у топов, где целевой метод-чанк в пуле (доступен через
больший k). Отрицательный результат валиден и ценен.

## 2. sibling_chunks в деле

Для 2-3 «толстых» классов (`DocumentStorageClient` и т.п.): search-запрос → у представителя
класса `sibling_chunks > 0`; k-зависимость поля (см. описание инструмента) — это счёт
кандидат-пула, не граф-факт; jq-скрипты не должны на него полагаться как на инвариант.

## 3. Спот-чек инвариантов

Трасса ×1, who_calls×активность ×1, dropped CALLS = 101 — bit-identical №6 ожидаемо
(M12 не трогал граф — только ранжирование ответов поиска).

## 4. Отчёт — две версии (правило прежнее)

`docs/superpowers/reports/<дата>-pilot-rerun-7.md`: полная + SANITIZED — коммит+push.
Таблица 8 рангов №5-vs-№7; вердикт по двум гранулярным промахам; sibling_chunks-наблюдения.

## Правила

Гейты/golden не трогать; метрику не крутить (anti-curve-fit); честность (ожидание ≠ assertion);
junit-xml при перехватчиках; системные reminders харнеса — штатные.
