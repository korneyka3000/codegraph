# Re-run №6 после M11 — classmethod-конструкции (SANITIZED)

> Дата: 2026-08-04. Санитизированная версия: числа, вердикты, обезличенные категории — без
> qualified-имён, путей и кода. Baseline — re-run №5 (R6 open-gaps). M11 закрыл R6: `(cls)`-суффикс
> переосмыслен как самоконструирование `cls(...)` внутри classmethod-фабрик → честный dst это узел
> класса. Три реальных Python-микросервиса (оркестратор на Temporal + два consumer-сервиса).

## 0. Смоук

Обновление подтянуто; local-emb; гейт **1 passed, 0 failed, 10.9s**. Индекс (тёплый кэш): **40.1s**
(0 fresh + cached).

## 1. ГЛАВНЫЙ чекпойнт — dropped CALLS (был 149, ожидание ~101)

**Факт: 149 → 101 (−48). Ожидание достигнуто точно.** Все 48 classmethod-дропов R6 восстановлены;
остаток classmethod-суффикс-дропов = **0**.

### 1.1. Природа восстановленных 48 — construction-рёбра «фабрика → КЛАСС»

Суффикс переосмыслен как самоконструирование `cls(...)` внутри classmethod-фабрики, поэтому честный
адресат — **узел класса** (как у прямого вызова конструктора), а не метод. Форма подтверждена:
рёбра `фабричный-классметод → узел-класса`, **static/1.0**, без mechanism-пропа, с callsite-счётчиком —
идентичны обычному конструктор-вызову. (Для скриптов: искать как CALLS с адресатом на узле класса,
источник — фабричный классметод; поиск по «метод-адресату» ничего не даст.)

### 1.2. Residual 101 — чистый динамический пол

| N | форма | природа |
|---|---|---|
| **79** | атрибут-фабрика (session) | динамика (рантайм-присвоение, установлено в №5) |
| 5 | callable-атрибуты | динамика |
| 3 | callable-**параметры** (higher-order) | динамика |
| 2 | произвольный callable (по дизайну) | честно |
| ~12 | локальные / разное | динамика |

Каждый остаток — **принципиально динамический** (нет статического узла-цели). Новой восстановимой
массы нет. by-design «произвольный callable»-дропов — **2** (бриф ждал ~1); оба честны, не
восстановимы.

### 1.3. Бонус — who_calls(Класс) показывает фабрики

Запрос «кто вызывает класс» теперь показывает фабричные классметоды наравне с обычными
конструктор-сайтами; construction-рёбра полноценно навигируемы.

## 2. Singleton provenance — невидим, парность держится

Синглтонов захарвестлено **92**, рёбер с механизмом singleton-dispatch — **0**. Парность «92/0» из
№5 сохранена (реального shadowing на корпусе не было). Recall-фикс relative-import-консюмеров не даёт
регрессий (92 без потерь).

## 3. Всё остальное — bit-identical

who_calls×активности (invokes_activity, без регрессий); трасса spot-check — confidence **1.00**,
external_exit 0, 6 сегментов (идентично №5); search несёт chunk_kind + enclosing_symbol; механизм
singleton-dispatch = 0. Граф-числа: HTTP 59 (23 static / 29 unresolved / 7 external), кросс-сегментов
43, префикс-роутер 0, send_unlinked 2, unresolved calls 4.9%, degraded=0.

## 4. Carry (пере-скоринг retrieval) — сделан в №5

Пере-скоринг трёх retrieval-промахов с новыми полями выполнен в №5; метрика hit@8 = 5/8 (M11 её не
трогает). Здесь не повторяется.

## 5. Вывод

**R6 закрыт точно; residual — чистый динамический пол.** dropped CALLS **149 → 101 (−48)**, ровно
ожидание. 48 восстановлены как construction-рёбра «фабрика → класс» (static/1.0, форма
конструктор-вызова); остаток суффикс-дропов = 0; бонус — who_calls(Класс) показывает фабрики.
Оставшиеся 101 — **все принципиально динамические** (атрибут-фабрика из №5, callable-атрибуты/
параметры/локали, 2 by-design); новой восстановимой массы нет — честный пол статического CALLS-резолва
на корпусе. Singleton-парность (92/0) и остальные инварианты bit-identical №5. Новых блокеров нет.
