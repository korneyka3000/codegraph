"""S7 linking: unifies staged Channel nodes, matches http-client claims onto the
cross-service route table, derives NEXT_SEGMENT/PART_OF_PROCESS, and materializes
BusinessProcess anchors. Entry point: `workspace.link_workspace`."""
